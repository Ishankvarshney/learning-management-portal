import { PreventDoubleClick } from './prevent-double-click';

describe('PreventDoubleClick', () => {
  it('should create an instance', () => {
    const directive = new PreventDoubleClick();
    expect(directive).toBeTruthy();
  });
});
